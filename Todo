11/17 회의 결과.
----------------------
(1) 나는 Server-manager에서 처리하는 로직에 따라 HttpResponse를 자동으로 생성해도록 하면 된다.
(2) --> [ HttpResponseManager ] class가 현재 상태보드(?) 에 입력된 flag를 받아서, 그걸 그대로 전달만 받으면 되지 않을까..?
(3) --> 사용자는 모르게. HRM이 알아서 상태를 입력(Status) 그 값들을 가지고 나머지는 HRM이 파일도 알아서 찾아서 연결하고...?


--> 아니다. HttpResponseManager는 cgi를 처리하지 않는다.  얘는 정말 데이터만 받는다. 